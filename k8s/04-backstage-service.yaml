apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: idp
  labels:
    app: backstage
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 7007
    protocol: TCP
  selector:
    app: backstage
  sessionAffinity: ClientIP
