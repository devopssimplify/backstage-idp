# Generate your own values:
#   echo -n "yourvalue" | base64
# Then apply: kubectl apply -f secret.yaml -n auth
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secret
  namespace: auth
type: Opaque
data:
  # Keycloak admin console credentials
  admin-username: YWRtaW4=        # admin
  admin-password: YWRtaW4xMjM=   # admin123  ← CHANGE THIS

  # PostgreSQL pod credentials (must match POSTGRES_PASSWORD in postgres.yaml)
  db-password: a2V5Y2xvYWsxMjM=  # keycloak123  ← CHANGE THIS
