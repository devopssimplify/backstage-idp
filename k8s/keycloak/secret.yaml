# Fill in base64-encoded values and apply with: kubectl apply -f secret.yaml -n auth
# Encode: echo -n "yourvalue" | base64
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secret
  namespace: auth
type: Opaque
data:
  admin-username: YWRtaW4=          # admin
  admin-password: Y2hhbmdlbWU=      # changeme  ← CHANGE THIS
  db-url: ""                         # jdbc:postgresql://<host>:5432/keycloak (leave empty for dev mode)
  db-username: ""                    # postgres user (leave empty for dev mode)
  db-password: ""                    # postgres pass (leave empty for dev mode)
